server:
  port: 8083

spring:
  application:
    name: pago-ms
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/medizano_pagos_db}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

paypal:
  base-url: ${PAYPAL_BASE_URL:https://api-m.sandbox.paypal.com}
  client-id: ${PAYPAL_CLIENT_ID:YOUR_PAYPAL_SANDBOX_CLIENT_ID}
  client-secret: ${PAYPAL_CLIENT_SECRET:YOUR_PAYPAL_SANDBOX_CLIENT_SECRET}
  currency: ${PAYPAL_CURRENCY:USD}
  exchange-rate: ${PAYPAL_EXCHANGE_RATE:3.75}

mercadopago:
  access-token: ${MERCADOPAGO_ACCESS_TOKEN:YOUR_MERCADOPAGO_ACCESS_TOKEN}
  public-key: ${MERCADOPAGO_PUBLIC_KEY:YOUR_MERCADOPAGO_PUBLIC_KEY}
  base-url: ${MERCADOPAGO_BASE_URL:https://api.mercadopago.com}
  currency: ${MERCADOPAGO_CURRENCY:PEN}
  webhook-secret: ${MERCADOPAGO_WEBHOOK_SECRET:}


eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
    distribution:
      percentiles-histogram:
        http.server.requests: true
